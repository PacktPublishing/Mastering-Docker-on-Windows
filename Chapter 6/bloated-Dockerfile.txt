# Example: intentionally bloated Dockerfile (for comparison)
FROM python:3.11

WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
RUN apt-get update && apt-get install -y vim curl net-tools
CMD ["python", "app.py"]
